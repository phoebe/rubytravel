<% @participation.each { |p| %>
<%= p.name %>
<% @user = User.find(p.user_id) %>
<%= @user.first_name %>
<%= @user.last_name %>
<% if p.user_id == current_user().id %>
  <% @profile= Profile.find(p.profile_id) %>
  <%= @profile.name %>
<% end %>
Travel Date: <%= p.traveldate %>
<% } %>

